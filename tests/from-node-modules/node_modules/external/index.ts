// These files are resolved by the typechecker
export * as tsmii from './typescript-module-imported-internally'
export * as jsmii from './javascript-module-imported-internally'
// These files are unknown to the compiler until required.
export const tsmri = require('./typescript-module-required-internally')
export const jsmri = require('./javascript-module-required-internally')
